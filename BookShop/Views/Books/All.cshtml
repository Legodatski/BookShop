@model IEnumerable<BookViewModel>

@if (Model.Count() == 0)
{
    <div>There are no studentbooks available at the moment</div>
}
else
{
    <div class="row">
        @foreach (var book in Model){
            <partial name="_BookPartial" model="@book"/>
        }
    </div>
}