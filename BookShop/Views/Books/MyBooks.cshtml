@model IEnumerable<BookViewModel>

@if (Model.Count() == 0)
{
    <div>You don't have any published books yet.</div>
}
else
{
    <div class="row">
        @foreach (var book in Model)
        {
            <partial name="_BookPartial" model="@book" />
        }
    </div>
}